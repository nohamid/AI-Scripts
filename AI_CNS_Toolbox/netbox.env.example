# Template for netbox.env - copy to netbox.env and fill in the token.
# This example file is safe to commit; netbox.env is not.
#   cp netbox.env.example netbox.env && chmod 600 netbox.env

NETBOX_URL=http://fkf-cns-spectre.cisco.com
NETBOX_TOKEN=
NETBOX_VERIFY_SSL=true
