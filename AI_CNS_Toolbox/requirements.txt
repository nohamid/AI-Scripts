Flask==2.3.3
Werkzeug==2.3.7
netmiko==4.3.0
icmplib==3.0.3
pyperclip==1.8.2
genie==23.8
pyats==23.8
pandas
python-pptx>=0.6.21
# SVG renderer used by CDP_to_PPTX. Two options are supported:
#   1) cairosvg  - fast and high quality, but needs the Cairo *system*
#      library. On macOS: `brew install cairo`. On Ubuntu/Debian:
#      `sudo apt-get install -y libcairo2 libpango-1.0-0 libpangocairo-1.0-0`.
#   2) svglib + reportlab - pure Python fallback, no system deps required.
# Install both so the script can fall back automatically:
cairosvg>=2.7.0
svglib>=1.5.1
reportlab>=4.0.0
pillow>=10.0.0